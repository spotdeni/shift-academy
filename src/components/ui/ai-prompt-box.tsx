import React from 'react';
import { motion } from 'framer-motion';
import { ArrowUp, Paperclip, X } from 'lucide-react';

type Props={value:string;onChange:(v:string)=>void;onSubmit:()=>void;placeholder?:string;disabled?:boolean};
export function PromptInputBox({value,onChange,onSubmit,placeholder='Напишите ответ…',disabled}:Props){const [file,setFile]=React.useState<File|null>(null);return <div className="rounded-2xl border border-slate-200 bg-white p-3 shadow-[0_12px_40px_rgba(37,99,235,.1)]"><textarea value={value} disabled={disabled} onChange={e=>onChange(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();onSubmit()}}} placeholder={placeholder} rows={3} className="w-full resize-none border-0 bg-transparent px-2 py-1 text-[15px] text-[#10243e] outline-none placeholder:text-slate-400"/><div className="flex items-center justify-between pt-2"><label className="flex cursor-pointer items-center gap-2 rounded-lg px-2 py-1.5 text-xs text-slate-500 hover:bg-slate-50"><Paperclip size={15}/><span>Прикрепить</span><input type="file" className="hidden" onChange={e=>setFile(e.target.files?.[0]||null)}/></label>{file&&<button onClick={()=>setFile(null)} className="mr-auto ml-2 text-slate-400"><X size={14}/></button>}<button onClick={onSubmit} disabled={disabled||!value.trim()} className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#2563eb] text-white transition hover:bg-[#1d4ed8] disabled:opacity-40"><ArrowUp size={17}/></button></div></div>}
